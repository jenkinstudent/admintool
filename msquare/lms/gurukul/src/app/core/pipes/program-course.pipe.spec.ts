import { ProgramCoursePipe } from './program-course.pipe';

describe('ProgramCoursePipe', () => {
  it('create an instance', () => {
    const pipe = new ProgramCoursePipe();
    expect(pipe).toBeTruthy();
  });
});
