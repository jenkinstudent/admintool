import { ProgramModulePipe } from './program-module.pipe';

describe('ProgramModulePipe', () => {
  it('create an instance', () => {
    const pipe = new ProgramModulePipe();
    expect(pipe).toBeTruthy();
  });
});
