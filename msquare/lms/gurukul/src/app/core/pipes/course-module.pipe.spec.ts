import { CourseModulePipe } from './course-module.pipe';

describe('CourseModulePipe', () => {
  it('create an instance', () => {
    const pipe = new CourseModulePipe();
    expect(pipe).toBeTruthy();
  });
});
