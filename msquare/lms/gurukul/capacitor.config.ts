import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.msquare.fmldemployee',
  appName: 'Fusion Gurukul',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
